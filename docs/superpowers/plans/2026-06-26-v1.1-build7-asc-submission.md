# v1.1 Build 7 ASC Submission Readiness — 2026-06-26

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to execute task-by-task.

**Repo:** `/Users/nadavyigal/Documents/Projects /ResumeBuilder/ResumeBuilder IOS APP`

**Goal:** Get build 7 (Fit-First + WP-18 instrumentation + the full activation redesign) archived, validated, and submitted to App Store Connect as v1.1 (7) — the redesign and D7-driven fixes are already implemented and QA'd; Codex completed the local verification gap in PR #84, and everything left is founder-only signing/upload plus post-live analytics verification.

**Context (verified against `tasks/progress.md` and git, 2026-06-26):**
- Live App Store build is v1.1 (5), confirmed 2026-06-21.
- Build 6 was prepped (Fit-First flag flipped ON per the D7 readout decision) but never uploaded.
- Build 7 bumped `CURRENT_PROJECT_VERSION` 6 → 7 to carry Fit-First (visible) + WP-18 upload-journey instrumentation + widened docx picker — the first build that will emit the new upload-funnel events that justified the redesign.
- The full Resumely Activation Redesign (Home upload-first hero, locked-tab teasers, Me/account trust-first redesign, first-score reveal restyle, target-reached/save-account wiring, connection-lost recovery view) is implemented, QA'd (8 issues found and fixed), and CodeRabbit-reviewed (PR #83 — 8 findings, 6 real ones fixed, 2 verified false positives) — all merged to `main`.
- Full test suite passes: 110 tests (105 XCTest + 5 Swift Testing), 0 failures. Debug build succeeds on iPhone 17 simulator.
- PR #84 closed the local build 7 preflight: the string catalog changes were real, selected-resume Hebrew visible/accessibility strings are filled, `HomeTabView` runtime-language fallback uses `NSLocalizedString(...)`, and the PR was squash-merged to `main` as `1caed0a`.
- iPhone SE visual smoke is complete on a fresh iPhone SE 3rd generation simulator (`A0F88614-B41A-4383-9C53-4C669CAE22A1`): Home EN/HE, upload hero, locked Optimized/Design/Expert teasers, and Me language/RTL surfaces passed after the localization fixes.
- **Blocker:** only an Apple Development cert exists in the local keychain — Distribution signing has never been resolved in Xcode. This blocks archive/upload and is the single thing standing between "code complete" and "submitted."
- **Remaining open gap:** founder-only App Store Connect signing/archive/upload/submit, then post-live PostHog WP-18 funnel verification.

---

## Task 1: Pre-flight check (completed in PR #84)

1. `git -C "<repo>" status --short --branch` — investigated the uncommitted `Localizable.xcstrings` change. It was real, not noise, and was committed through PR #84.
2. `git -C "<repo>" log --oneline -10` — confirmed `main` had PR #83 (CodeRabbit fix pass), PR #82 (redesign export), PR #81 (build 7 bump), PR #80 (WP-18), and Fit-First PR history.
3. Confirmed `CURRENT_PROJECT_VERSION` is 7 and `MARKETING_VERSION` is 1.1 in the Xcode project settings.
4. Ran the full test suite fresh: `xcodebuild test -project "ResumeBuilder IOS APP.xcodeproj" -scheme "ResumeBuilder IOS APP" -destination "platform=iOS Simulator,name=iPhone 17"` passed 105 XCTest + 5 Swift Testing tests.

**Success criteria:** Met in PR #84. Clean/knowingly resolved git state, version confirmed 7/1.1, fresh test run green.

## Task 2: iPhone SE visual smoke (completed in PR #84)

This was never run anywhere in the prior sessions — close it before submission, not after, since visual regressions on a smaller screen are exactly the kind of thing App Review or real users catch first.

1. Created a fresh iPhone SE 3rd generation simulator on iOS 26.5.
2. Walked the activation-redesign surfaces available without production account state: Home upload-first hero/progress strip, upload CTA area, locked Optimized/Design/Expert teasers, and Me/account trust-first redesign in EN and HE/RTL.
3. Found visible Hebrew fallback strings on the Home/Me/locked teaser surfaces and fixed the release-critical catalog entries, including selected-resume visible/accessibility copy during PR #84 review.
4. Final Hebrew Home screenshot was saved at `/tmp/resumely-build7-se-smoke/11-home-he-final-nslocalized.png`.

**Success criteria:** Met. iPhone SE smoke passed for the checked surfaces, with fixed Hebrew fallbacks committed and merged.

## Task 3: Founder-only — resolve signing and submit (external, not Codex-executable)

This requires the founder's Apple Developer account access and cannot be automated:

- [ ] Founder: open Xcode → Signing & Capabilities → resolve Apple Distribution certificate (Automatic signing, or manually create/download the Distribution cert if Automatic doesn't trigger it)
- [ ] Founder: Product → Archive
- [ ] Founder: Organizer → Validate App
- [ ] Founder: Organizer → Distribute App → App Store Connect
- [ ] Founder: in ASC, fill Promotional Text / What's New (mention Fit-First Triage + the redesign — first time either is user-visible at scale)
- [ ] Founder: submit 1.1 (7) for review

## Task 4: Post-submission verification

1. Once Apple approves and the build goes live, confirm via the actual App Store listing (not just ASC status) that v1.1 (7) is what's installable.
2. Smoke-test the upload-funnel instrumentation fires in production PostHog (project 270848): `resume_upload_cta_tapped`, `resume_file_picker_opened`, `resume_file_selected`, `resume_upload_succeeded` — this is the entire point of WP-18, confirm it's not silently broken in production the way the pre-WP-18 funnel was.
3. Update `tasks/progress.md` with the live confirmation and a fresh D7 readout date target (7 days from live date) so the next activation read has a clean cohort.

---

## What This Plan Does NOT Cover

- Backend/state follow-ups already honestly flagged and parked: paste-text diagnosis, sample/demo diagnosis, parser-stage progress callbacks, point-delta apply-all fixes, resumable offline analysis, true NWPathMonitor-driven auto-resume (current Retry is manual only).
- The metric-nudge UX feature decision for ResumeBuilder Web (Story 2 follow-up) — that's a web-repo, founder-decision item, not part of getting this iOS build out.
- Any new feature scope beyond what's already merged to `main`.
