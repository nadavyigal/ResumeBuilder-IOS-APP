# iOS QA Report — PR #58 Resume Aha Moments

Date: 2026-06-14
Branch: `codex/resume-aha-moments`
PR: #58 `[codex] Add resume aha moments`
Status: Partial pass — code/tests/archive pass, live smoke blocked

## Summary

PR #58 was reviewed before merge. Actionable review issues were remediated around stable diagnosis navigation state, Swift concurrency isolation, duplicate chip identifiers, CTA gating, stale backend diagnosis context, loading-state panel visibility, and DTO decoding coverage.

The app builds and archives successfully, and the automated test suite is green. The remaining release blocker is interactive smoke testing: CoreSimulator hung on install/screenshot/container commands, so the changed UI and delete-account/re-register flow were not verified live in this session.

## Verification Completed

- `git diff --check` passed.
- Focused test passed: `ResumeBuilder IOS APPTests/ResumeDiagnosisViewModelTests`, 7 tests, 0 failures.
- Full test suite passed on iPhone 17 simulator: 83 XCTest tests plus 5 Swift Testing tests, 0 failures.
- Release archive succeeded: `/tmp/ResumeBuilder-PR58.xcarchive`.
- Release build settings reviewed:
  - `MARKETING_VERSION = 1.0`
  - `CURRENT_PROJECT_VERSION = 4`
  - `API_BASE_URL = https://www.resumelybuilderai.com`
  - bundle id `Resumebuilder-IOS.ResumeBuilder-IOS-APP`
  - Sign in with Apple entitlement present.
- Account deletion and registration source paths reviewed:
  - `AppState.deleteAccount()` calls backend account deletion, tracks deletion, then signs out.
  - `AuthService.deleteAccount(accessToken:)` calls `functions/v1/delete_account` with anon key and bearer token, requires a successful response, and clears session.
  - `ProfileView` exposes destructive Delete Account only for signed-in users and uses a confirmation alert.
  - `OnboardingView` supports register/sign-in toggle and account creation.

## Not Completed

- Live simulator smoke for the new diagnosis UI.
- Live App Store compliance flow: register, delete account, register again.
- Live upload/optimize/export smoke with authenticated backend data.

## Blocker

CoreSimulator became unhealthy after the automated tests passed. `xcrun simctl install`, `xcrun simctl io screenshot`, `xcrun simctl get_app_container`, and XcodeBuildMCP install/snapshot operations hung or timed out against the iPhone 17 iOS 26.5 simulator.

## Release Notes

The CLI archive used an Apple Development signing identity and team provisioning profile. This is enough to prove the project archives, but App Store submission still needs validation/export/upload from Xcode Organizer with App Store distribution signing.

Existing non-blocking warnings observed:

- `UIDeviceFamily` is supplied in `Info.plist` and overwritten by Xcode; project should eventually remove it and rely on `TARGETED_DEVICE_FAMILY`.
- `AppState.swift` has an existing unused `urlError` warning.

## Recommendation

Do not merge for App Store submission until a live smoke test passes on a healthy simulator or real device, especially delete account and register again. After that, validate and distribute the archive from Xcode Organizer.
